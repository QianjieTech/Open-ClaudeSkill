# Open-ClaudeSkill 项目优化设计方案

## 1. 项目现状分析

### 1.1 核心问题识别
- **静态配置**：技能目录需要手动指定，无法自动适应当前项目环境
- **上下文缺失**：MCP 服务器无法感知当前工作目录和项目结构
- **配置复杂**：不同项目间需要手动修改路径配置
- **错误风险**：路径配置错误导致技能加载失败

### 1.2 技术架构现状
- **src/mcp_server_skill/server.py**：MCP 服务器主逻辑，文件监控
- **src/mcp_server_skill/skill_loader.py**：技能发现、解析、管理
- **当前模式**：基于启动参数的静态路径配置

### 1.3 现状限制
- MCP 服务器生命周期与客户端一对一
- stdio 通信模式，无状态服务设计
- 缺乏项目环境感知能力

## 2. 优化目标

### 2.1 核心目标
- **环境自适应**：自动感知当前项目环境
- **零配置启动**：无需手动指定技能目录
- **状态管理**：支持项目上下文状态
- **无缝集成**：与现有 AI 代理兼容

### 2.2 用户体验目标
- 项目切换时自动适应
- 减少配置错误
- 操作简便性

## 3. 架构优化设计

### 3.1 新增初始化工具
- 添加 `set_project_context` 工具，用于初始化项目环境上下文
- 该工具接收项目根目录路径作为参数
- 服务器接收后设置当前项目上下文，后续操作基于此上下文

### 3.2 状态管理机制
- 引入服务器状态类管理项目上下文
- 技能加载器支持动态上下文更新
- 服务器维护当前项目路径和技能目录的状态

### 3.3 智能技能目录发现
- 实现项目根检测算法，支持多种项目类型识别
- 自动发现机制按优先级搜索技能目录
- 支持向父目录递归搜索直到找到项目标记

## 4. 实现架构

### 4.1 状态化服务器重构
- 重构服务器类以支持状态管理
- 添加项目上下文设置处理器
- 实现状态同步和监控重启机制

### 4.2 项目上下文管理
- 实现上下文设置工具的处理器逻辑
- 支持项目路径验证和上下文更新
- 集成文件监控的动态重启机制

### 4.3 文件监控增强
- 支持动态监控目录切换
- 实现监控重启功能
- 优化文件变化处理逻辑

## 5. API 接口设计

### 5.1 初始化接口
- 新增 `set_project_context` 接口，接收项目路径参数
- 返回项目上下文设置结果

### 5.2 技能加载接口
- 保持现有 `load_skill` 接口兼容性
- 操作基于当前服务器状态中的项目上下文

## 6. 配置兼容性处理

### 6.1 向后兼容
- 保留原命令行参数支持
- 支持环境变量配置方式
- 默认行为保持兼容

### 6.2 优先级策略
- 初始化工具设置（最高优先级）
- 命令行参数
- 环境变量
- 自动发现（默认）

## 7. 错误处理与日志

### 7.1 错误处理策略
- 项目路径不存在时的友好提示
- 权限不足时的错误处理
- 技能解析错误的详细日志

### 7.2 日志增强
- 项目上下文切换日志
- 技能目录变化监控日志
- 错误调试信息

## 8. 部署与集成

### 8.1 Agent 集成方案
- Agent 启动时检测当前项目路径
- 调用 set_project_context 传递项目路径
- 服务器设置项目上下文，开始监控
- 后续 load_skill 调用基于项目上下文

### 8.2 向后兼容部署
- 无需修改现有 Agent 配置即可使用
- 支持逐步迁移到新功能

## 9. 测试策略

### 9.1 单元测试
- 状态管理逻辑测试
- 路径发现算法测试
- 工具调用处理测试

### 9.2 集成测试
- 项目切换场景测试
- 文件监控状态同步测试
- 向后兼容性测试

## 10. 性能考虑

### 10.1 内存管理
- 状态数据合理缓存
- 内存泄漏防护

### 10.2 监控效率
- 文件系统监控优化
- 避免重复扫描

## 11. 总结

此优化方案通过以下关键改进解决了现有问题：

1. **有状态服务器**：支持项目上下文感知
2. **智能路径发现**：减少手动配置需求
3. **初始化工具**：标准的上下文传递机制
4. **向后兼容**：保持现有功能的兼容性
5. **无状态冲突**：利用 MCP 1:1 连接特性

该方案既解决了当前的痛点，又保持了架构的简洁性和兼容性。