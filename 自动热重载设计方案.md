# MCP Skill Server 设计文档

## 1. 核心理念

### 1.1 设计目标

创建一个基于 MCP 协议的技能管理系统，实现：

- **零配置体验**：技能文件即服务，无需繁琐配置
- **实时热重载**：文件变更即刻生效，无需重启对话
- **渐进式披露**：按需加载内容，优化 token 使用
- **跨平台兼容**：标准 MCP 协议，适配所有 Agent 应用

### 1.2 核心价值主张

将 Claude Skill 从 Kilo Code 专属功能转变为通用的、跨平台的知识管理方案，让任何支持 MCP 的 Agent 都能享受结构化的专业知识注入能力。

---

## 2. 技术架构设计

### 2.1 协议选型：纯 MCP 标准

- **无专有扩展**：100% 基于 MCP 协议规范实现
- **未来兼容**：随协议演进自动获得新特性
- **广泛支持**：兼容所有符合 MCP 标准的 Client

### 2.2 工具暴露模式：动态工具列表

#### 核心机制

每个 skill 文件映射为一个独立的 MCP 工具：

```
.skill/code-review.md  →  tool: skill_code_review
.skill/test-gen.md     →  tool: skill_test_gen
.skill/refactoring.md  →  tool: skill_refactoring
```

#### 设计优势

- **自然发现**：Agent 在工具列表中直接看到所有可用 skill
- **智能匹配**：Agent 根据工具名称和描述自动选择合适的 skill
- **无需提示**：不依赖特殊的 system prompt 约束
- **官方体验**：与原生 Claude Skill 行为一致

### 2.3 实时热重载机制

#### 变更检测

- 监控 `.skill/` 目录的文件系统事件
- 检测 `.md` 文件的增删改操作
- 维护技能列表的内存缓存

#### 动态通知

利用 MCP 协议的 `notifications/tools/list_changed` 机制：

```
文件变更 → 重新扫描目录 → 发送协议通知 → Client 重新获取工具列表 → Agent 更新上下文
```

#### 关键能力声明

在初始化时声明 `capabilities.tools.listChanged: true`，告知 Client 支持工具列表动态更新。

---

## 3. 环境感知设计

### 3.1 工作目录问题分析

#### 现状挑战

不同 MCP Client 对 Server 工作目录的处理行为不统一：

- **Kilo Code**：通常使用项目根目录
- **Claude Desktop**：可能是应用安装目录或配置指定路径
- **其他 Agent**：行为各异，无统一标准

#### 核心矛盾

Server 需要定位 `.skill/` 目录，但无法预知自己的工作目录在何处。

### 3.2 解决方案：多策略回退

#### 策略优先级

1. **命令行参数**：`--skills-dir /path/to/.skill`
2. **环境变量**：`SKILL_DIR=/path/to/.skill`
3. **初始化参数**：通过 `initialize` 请求传递
4. **工作目录探测**：检查 `cwd` 下是否存在 `.skill/`
5. **向上查找**：递归查找父目录中的 `.skill/`
6. **运行时设置**：提供工具让用户/Agent 手动指定

#### 策略 6 详解：运行时协商

提供专用工具用于会话级别的目录设置：

**工具定义**：

- 名称：`set_skills_directory`
- 用途：在对话开始时由 Agent 根据上下文信息设置技能目录
- 时机：首次调用任何 skill 工具前
- 持久性：会话级别，支持对话中切换项目

**配合 Prompt 约束**：
在 Agent 的 system prompt 中约定：

- 对话开始时，通过上下文信息（如用户当前项目路径）调用此工具
- 仅需调用一次，后续自动使用已设置的路径
- 支持对话中途切换项目（重新调用即可）

#### 优势分析

- **跨平台兼容**：不依赖任何特定 Client 的行为
- **用户友好**：通过 LLM 的上下文传递，用户无需手动配置
- **灵活可控**：支持多项目场景和运行时切换
- **优雅降级**：即使自动探测失败，也有明确的设置路径

---

## 4. 性能优化设计

### 4.1 智能阈值切换

#### 问题场景

- 少量 skill（≤20）：适合全部作为独立工具暴露
- 大量 skill（>20）：工具列表过长，影响 Agent 决策效率

#### 自适应策略

根据 skill 数量动态选择暴露模式：

**模式 A：直接暴露（≤20 个）**

- 每个 skill 作为独立工具
- Agent 直接看到所有选项
- 最佳用户体验

**模式 B：加载器模式（>20 个）**

- 提供两个通用工具：
  - `list_available_skills`：列出所有可用 skill
  - `load_skill(name)`：加载指定 skill
- Agent 先查询列表，再按需加载
- 节省上下文空间

### 4.2 缓存策略

#### 目录扫描缓存

- 启动时全量扫描一次
- 文件变更时局部更新
- 避免频繁的磁盘 I/O

#### 内容懒加载

- 仅在工具被实际调用时读取 skill 内容
- frontmatter 元数据可提前解析用于描述
- 支持大量 skill 而不占用内存

---

## 5. Skill 格式规范

### 5.1 兼容性承诺

100% 兼容官方 Claude Skill 格式：

- YAML frontmatter 元数据
- Markdown 正文内容
- `allowed-tools` 字段（保留但说明适用范围）

### 5.2 元数据利用

#### name（必需）

- 用途：生成工具名称（`skill_{name}`）
- 要求：小写字母、数字、连字符

#### description（必需）

- 用途：工具描述，Agent 用于决策
- 最佳实践：简洁明确，50-150 字符

#### allowed-tools（可选）

- 说明：此为 Kilo Code 专有字段
- 行为：其他 Agent 会安全忽略
- 建议：保留以保持格式兼容性

---

## 6. 用户体验设计

### 6.1 零配置启动流程

**理想路径（Kilo Code）**：

1. 用户在项目中创建 `.skill/` 目录
2. 添加 skill 文件
3. 启动 Agent
4. 立即可用，无需任何配置

**通用路径（其他 Agent）**：

1. 用户添加 Server 到配置
2. 对话开始时，Agent 通过 `set_skills_directory` 设置路径
3. 文件变更自动生效
4. 无需重启对话

### 6.2 错误处理友好性

#### 目录未设置

提示信息示例：

```
技能目录未配置。请告诉我你的项目路径，例如：
"我的项目在 /Users/me/my-project"
```

#### Skill 文件格式错误

提供详细的解析错误信息：

- 具体的 YAML 语法错误位置
- Markdown 格式问题说明
- 修复建议

#### 文件不存在

智能建议：

- 列出可用的 skill 名称
- 提示可能的拼写错误
- 建议使用 `list_available_skills`

---

## 7. 多项目支持设计

### 7.1 实例隔离方案

#### 配置示例

用户可为不同项目配置独立的 Server 实例：

```json
{
  "mcpServers": {
    "skills-work": {
      "command": "uvx",
      "args": ["mcp-server-skill", "--skills-dir", "~/work/.skill"]
    },
    "skills-personal": {
      "command": "uvx", 
      "args": ["mcp-server-skill", "--skills-dir", "~/personal/.skill"]
    }
  }
}
```

#### 工具命名策略

为避免命名冲突，支持 `--instance-id` 参数：

```
skill_code_review      →  work_skill_code_review
skill_test_generator   →  personal_skill_test_generator
```

### 7.2 会话级切换（推荐）

#### 单实例多项目

仅启动一个 Server 实例，通过 `set_skills_directory` 在对话中切换：

```
对话 A：处理工作项目
  → set_skills_directory("~/work/.skill")
  → 使用工作相关 skills

对话 B：处理个人项目  
  → set_skills_directory("~/personal/.skill")
  → 使用个人 skills
```

#### 优势

- 资源占用更少（单一进程）
- 配置更简单（无需多实例）
- 灵活性更高（随时切换）

---

## 8. 文档与引导策略

### 8.1 分层文档设计

#### README（概览）

- 5 分钟快速开始
- 核心特性展示
- 典型使用场景

#### AGENT_PROMPT.md（集成指南）

- 针对不同 Agent 的 system prompt 模板
- 环境感知的最佳实践
- 故障排查指南

#### SKILL_FORMAT.md（格式规范）

- 详细的 frontmatter 字段说明
- Markdown 编写建议
- 示例 skill 模板

### 8.2 平台特定说明

为主流 Agent 提供定制化配置示例：

- **Kilo Code**：强调零配置特性
- **Claude Desktop**：提供完整配置示例
- **Cline**：说明路径设置方式
- **其他**：通用配置模板

---

## 9. 安全性考虑

### 9.1 路径遍历防护

- 验证 skill 目录路径的合法性
- 拒绝包含 `..` 的路径
- 限制访问范围在指定目录内

### 9.2 文件类型限制

- 仅处理 `.md` 文件
- 忽略隐藏文件（`.` 开头）
- 跳过符号链接以防路径逃逸

### 9.3 资源限制

- 单个 skill 文件大小限制（如 1MB）
- 总 skill 数量限制（如 1000 个）
- 防止恶意的大量文件攻击

---

## 10. 未来演进方向

### 10.1 协议增强

- 支持 skill 依赖关系声明
- 版本管理和兼容性检查
- 远程 skill 仓库支持

### 10.2 功能扩展

- Skill 模板市场
- 使用统计和推荐
- 跨项目 skill 共享

### 10.3 性能优化

- 索引和全文搜索
- 增量更新机制
- 分布式 skill 管理

---

## 11. 成功指标

### 11.1 技术指标

- ✅ 工具列表更新延迟 < 1 秒
- ✅ 支持至少 100 个 skill 无性能下降
- ✅ 兼容至少 3 个主流 MCP Client

### 11.2 体验指标

- ✅ 零配置场景下 5 分钟内完成首次使用
- ✅ 文件变更到生效全过程用户无感知
- ✅ 错误提示能直接指导用户完成修复

---

## 附录：术语表


| 术语       | 定义                                         |
| ---------- | -------------------------------------------- |
| Skill      | 包含结构化知识的 Markdown 文件               |
| MCP        | Model Context Protocol，Claude 的扩展协议    |
| Agent      | 支持 MCP 协议的 AI 应用（如 Claude Desktop） |
| Server     | 实现 MCP 协议的服务端程序                    |
| Client     | 发起 MCP 请求的客户端（Agent）               |
| Tool       | MCP 中的可调用功能单元                       |
| Hot Reload | 文件变更后无需重启即可生效                   |
| Session    | 一次完整的对话会话                           |

---

**文档版本**：1.0
**最后更新**：2025-11-12
**状态**：待开发实现
